{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://reward-layer-protocol.github.io/rlp/schemas/manifest.json",
  "$comment": "NON-NORMATIVE: Derived from spec/rlp.ts. See SPECIFICATION.md Section 4 for authoritative definitions.",
  "title": "RLP Manifest",
  "description": "Task manifest served at /.well-known/agent-reward.json",
  "type": "object",
  "required": ["version", "issuer", "tasks"],
  "properties": {
    "version": {
      "type": "string",
      "description": "Protocol version (e.g., '1.1')"
    },
    "issuer": {
      "type": "string",
      "format": "uri",
      "description": "URL identifying the manifest issuer"
    },
    "tasks": {
      "type": "array",
      "description": "Available tasks (may be empty)",
      "items": {
        "$ref": "task.json"
      }
    },
    "claimInstructions": {
      "type": "object",
      "description": "Instructions for submitting claims",
      "properties": {
        "method": {
          "type": "string",
          "const": "POST"
        },
        "contentType": {
          "type": "string",
          "const": "application/json"
        },
        "bodySchema": {
          "type": "object",
          "properties": {
            "output": {
              "type": "string",
              "description": "Description of the output field"
            },
            "agentId": {
              "type": "string",
              "description": "Description of the agentId field"
            }
          }
        }
      }
    },
    "extensions": {
      "type": "array",
      "description": "Optional list of extensions supported by this manifest",
      "items": {
        "$ref": "#/$defs/Extension"
      }
    }
  },
  "$defs": {
    "Extension": {
      "type": "object",
      "required": ["uri"],
      "properties": {
        "uri": {
          "type": "string",
          "description": "Unique URI identifying the extension"
        },
        "config": {
          "type": "object",
          "additionalProperties": true,
          "description": "Extension-specific configuration"
        }
      }
    }
  }
}
